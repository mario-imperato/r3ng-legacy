<< BOF Changing rows at: 1
// GPA Gruppo Progetti Avanzati s.r.l. - 
// Progetto UNEP 2012 per PosteItaliane S.p.a.
// generated by jGAMe Software - GPA Gruppo Progetti Avanzati s.r.l. 2016-2017
<< BOF Changing rows at: 5
// TODO: Readme Section.
// README Section
<< BOF Deleting rows at: 8

<< BOF Changing rows at: 23
      var adapter = new org.r3.db.DTOTableAdapter(serverResponseData, function(aRowDataBean) { return new org.r3.db.sailevent.appcrewmemberview.SailApplicationCrewMemberViewDTOAdapter(aRowDataBean); });   
      var adapter = new org.r3.db.DTOTableAdapter(serverResponseData, function(aRowDataBean) { return new org.r3.db.sailevent.appcrewmemberview.SailApplicationCrewMemberViewDTOAdapter(aRowDataBean); });
<< BOF Deleting rows at: 26
   
   _setupDefaultColumnDefinitions: function()
   { 
   	   var defaultColumnDefinitions = {};
   	      
   	
	   defaultColumnDefinitions['memberinfo'] = 
       {  
       	  "aTargets": [ -1 ], 
       	  "sClass": "center", 
       	  "sTitle": "Nominativo / Circolo",
          "render": function ( data, type, row, meta ) 
          {
          	    var item = data; 
                return item.getMemberInfo() || '';
          }
       };
<< BOF Changing rows at: 44
   	   defaultColumnDefinitions['indexofitem'] = 
       {  
       	  "aTargets": [ -1 ], 
       	  "sClass": "center", "sTitle": "",
          "render": function ( data, type, row, meta ) 
          {
          	    var item = data; 
                return item.indexOfItem;
          }
       };

        defaultColumnDefinitions['crewmemberid'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "crewmemberid",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.crewmemberid || '';
          }       
       };
        defaultColumnDefinitions['eventid'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "eventid",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.eventid || '';
          }       
       };
        defaultColumnDefinitions['applicationid'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "applicationid",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.applicationid || '';
          }       
       };
        defaultColumnDefinitions['cardnumber'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "Numero",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.cardnumber || '';
          }       
       };
        defaultColumnDefinitions['crewmembername'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "Nominativo",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.crewmembername || '';
          }       
       };
        defaultColumnDefinitions['weight'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "weight",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.weight || '';
          }       
       };
        defaultColumnDefinitions['crewmemberrolecode'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "Ruolo",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.getCrewmemberrolecode() || '';
          }       
       };
        defaultColumnDefinitions['memberstatuscode'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "Tit/Ris.",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.getMemberstatuscode() || '';
          }       
       };
        defaultColumnDefinitions['createtimestamp'] = 
        {
        	  "aTargets": [ -1 ],
           "sClass": "left",
           "sTitle": "createtimestamp",
           "render": function ( data, type, row, meta )
           {
              var item = data; 
              return item.dataBean.createtimestamp || '';
           }       
        };
         defaultColumnDefinitions['deletetimestamp'] = 
        {
        	  "aTargets": [ -1 ],
           "sClass": "left",
           "sTitle": "deletetimestamp",
           "render": function ( data, type, row, meta )
           {
              var item = data; 
              return item.dataBean.deletetimestamp || '';
           }       
        };
         defaultColumnDefinitions['checkcardnumber'] = 
        {
        	  "aTargets": [ -1 ],
           "sClass": "left",
           "sTitle": "&nbsp;",
           "render": function ( data, type, row, meta )
           {
              var item = data; 
              return item.getCheckcardnumberAsSemaphore() || '';
           }       
        };
         defaultColumnDefinitions['checkcardnumber_ts'] = 
        {
        	  "aTargets": [ -1 ],
           "sClass": "left",
           "sTitle": "Data Verif.",
           "render": function ( data, type, row, meta )
           {
              var item = data; 
              return item.getCheckcardnumber_ts() || '';
           }       
        };        
        defaultColumnDefinitions['recstatus'] = 
        {
        	  "aTargets": [ -1 ],
           "sClass": "left",
           "sTitle": "recstatus",
           "render": function ( data, type, row, meta )
           {
              var item = data; 
              return item.dataBean.recstatus || '';
           }       
        };        
        defaultColumnDefinitions['rectimestamp'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "Ultima Mod.",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.getRectimestamp() || '';
          }       
       };
        defaultColumnDefinitions['cardnumberstatuscode'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "&nbsp;",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.getCardnumberstatuscodeAsSemaphore() || '';
          }       
       };
        defaultColumnDefinitions['yachtclub'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "yachtclub",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.yachtclub || '';
          }       
       };
        defaultColumnDefinitions['cardholdername'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "cardholdername",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.cardholdername || '';
          }       
       };
        defaultColumnDefinitions['cardholderlastname'] = 
        {
        	  "aTargets": [ -1 ],
           "sClass": "left",
           "sTitle": "cardholderlastname",
           "render": function ( data, type, row, meta )
           {
              var item = data; 
              return item.dataBean.cardholderlastname || '';
           }       
        };        
        defaultColumnDefinitions['medexamexpirationdate'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "Vis. Med.",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.getMedexamexpirationdate() || '';
          }       
       };
        defaultColumnDefinitions['cardexpirationdate'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "cardexpirationdate",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.cardexpirationdate || '';
          }       
       };
        defaultColumnDefinitions['renewaldate'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "Rinnovo",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.getRenewaldate() || '';
          }       
       };
        defaultColumnDefinitions['generalcardexpirationdate'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "generalcardexpirationdate",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.generalcardexpirationdate || '';
          }       
       };
        defaultColumnDefinitions['systemcheckdate'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "Data Verif. FIV",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.getSystemcheckdate() || '';
          }       
       };
        defaultColumnDefinitions['sailnumber'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "sailnumber",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.sailnumber || '';
          }       
       };
        defaultColumnDefinitions['yachtname'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "yachtname",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.yachtname || '';
          }       
       };
        defaultColumnDefinitions['completionstatus'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "completionstatus",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.completionstatus || '';
          }       
       };
        defaultColumnDefinitions['verificationstatus'] = 
       {
       	  "aTargets": [ -1 ],
          "sClass": "left",
          "sTitle": "verificationstatus",
          "render": function ( data, type, row, meta )
          {
             var item = data; 
             return item.dataBean.verificationstatus || '';
          }       
       };
        
       return defaultColumnDefinitions;
   _setupDefaultColumnDefinitions: function()
   {
       return org.r3.db.sailevent.appcrewmemberview.SailApplicationCrewMemberViewQueryActionFormView_ColumnDefinitions (this.dataTableId);
<< BOF Changing rows at: 355
   

<< BOF Changing rows at: 358
   	  var adaptedItem = new org.r3.db.sailevent.appcrewmemberview.SailApplicationCrewMemberViewDTOAdapter(anItem);   
   	  var adaptedItem = new org.r3.db.sailevent.appcrewmemberview.SailApplicationCrewMemberViewDTOAdapter(anItem);
<< BOF Changing rows at: 361
   

<< BOF Changing rows at: 366
 

<< BOF Inserting rows at: 369
org.r3.db.sailevent.appcrewmemberview.SailApplicationCrewMemberViewQueryActionFormView_ColumnDefinitions  = function(theDataTableId, includeOnlyFieldList)
{
    var defaultColumnDefinitions = {};

    var attributekey = '<attributeid>';
    var global_multiselector = theDataTableId + "_all_" + attributekey;
    var selectorPrefix = theDataTableId;

    var columnId = 'singleselector';
    if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
   		defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
    	  	"sClass": "text-center",
    	  	"sTitle": "",
      		"render": function ( data, type, row, meta )
      		{
      	    	var item = data;
      	    	var k = item.dataBean.dtokey;
      	    	var d = item.dataBean.dtodescr;

      	    	var itemChecked = "";
//      	    if ($('#' + global_multiselector).prop('checked'))
//      	    {
//      	    	itemChecked = ' checked ';
//      	    }
      	    	var s = "<input type='radio' name='" + attributekey + "' id='" + selectorPrefix + '_' + item.dataBean.dtokey + "' value='" + k + "' title='" + d + "' " + itemChecked + " />";
            	return s;
      		}
    	};
    }

	var columnId = 'multiselector';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
			"aTargets": [ -1 ],
    		"sClass": "text-center",
    		"sTitle": "<input type='checkbox' id='" + global_multiselector + "' name='all_" + attributekey + "' value='btrue' />",

       		"render": function ( data, type, row, meta )
       		{
       	    	var item = data;
       	    	var k = item.dataBean.dtokey;
       	    	var d = item.dataBean.dtodescr;

       	    	var itemChecked = "";
//       	    if ($('#' + global_multiselector).prop('checked'))
//       	    {
//       	    	itemChecked = ' checked ';
//       	    }
       	    	var s = "<input type='checkbox' name='" + attributekey + "' value='" + k + "' " + itemChecked + " />";
             	return s;
       		}
    	};
    }

	var columnId = 'indexofitem';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
	   	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
    	  	"sClass": "text-center",
    	  	"sTitle": "",
			"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.indexOfItem;
       		}
    	};
    }

	var columnId = 'queryformview-tools';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    		"aTargets": [ -1 ],
    	  	"sClass": "text-center",
    	  	"sTitle": "",
    	  	"sWidth": "30px",
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		var s = '<div class="queryformview-tools"><i class="fa fa-file-o tools-icon toolid-file" style="margin: 2px"></i></div>';
             	return s;
       		}
    	};
    }

	var columnId = 'crewmemberid';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.crewmemberid,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.crewmemberid || '';
       		}
    	};
    }
 	var columnId = 'eventid';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.eventid,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.eventid || '';
       		}
    	};
    }
 	var columnId = 'applicationid';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.applicationid,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.applicationid || '';
       		}
    	};
    }
 	var columnId = 'cardnumber';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.cardnumber,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.cardnumber || '';
       		}
    	};
    }
 	var columnId = 'crewmembername';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.crewmembername,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.crewmembername || '';
       		}
    	};
    }
 	var columnId = 'weight';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.weight,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.weight || '';
       		}
    	};
    }
 	var columnId = 'crewmemberrolecode';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.crewmemberrolecode,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.crewmemberrolecode || '';
       		}
    	};
    }
 	var columnId = 'memberstatuscode';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.memberstatuscode,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.memberstatuscode || '';
       		}
    	};
    }
 	var columnId = 'createtimestamp';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.createtimestamp,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.createtimestamp || '';
       		}
    	};
    }
 	var columnId = 'deletetimestamp';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.deletetimestamp,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.deletetimestamp || '';
       		}
    	};
    }
 	var columnId = 'checkcardnumber';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.checkcardnumber,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.checkcardnumber || '';
       		}
    	};
    }
 	var columnId = 'checkcardnumber_ts';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.checkcardnumber_ts,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.checkcardnumber_ts || '';
       		}
    	};
    }
 	var columnId = 'recstatus';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.recstatus,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.recstatus || '';
       		}
    	};
    }
 	var columnId = 'rectimestamp';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.rectimestamp,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.rectimestamp || '';
       		}
    	};
    }
 	var columnId = 'cardnumberstatuscode';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.cardnumberstatuscode,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.cardnumberstatuscode || '';
       		}
    	};
    }
 	var columnId = 'yachtclub';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.yachtclub,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.yachtclub || '';
       		}
    	};
    }
 	var columnId = 'cardholdername';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.cardholdername,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.cardholdername || '';
       		}
    	};
    }
 	var columnId = 'cardholderlastname';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.cardholderlastname,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.cardholderlastname || '';
       		}
    	};
    }
 	var columnId = 'medexamexpirationdate';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.medexamexpirationdate,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.medexamexpirationdate || '';
       		}
    	};
    }
 	var columnId = 'cardexpirationdate';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.cardexpirationdate,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.cardexpirationdate || '';
       		}
    	};
    }
 	var columnId = 'renewaldate';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.renewaldate,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.renewaldate || '';
       		}
    	};
    }
 	var columnId = 'generalcardexpirationdate';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.generalcardexpirationdate,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.generalcardexpirationdate || '';
       		}
    	};
    }
 	var columnId = 'systemcheckdate';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.systemcheckdate,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.systemcheckdate || '';
       		}
    	};
    }
 	var columnId = 'sailnumber';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.sailnumber,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.sailnumber || '';
       		}
    	};
    }
 	var columnId = 'yachtname';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.yachtname,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.yachtname || '';
       		}
    	};
    }
 	var columnId = 'completionstatus';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.completionstatus,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.completionstatus || '';
       		}
    	};
    }
 	var columnId = 'verificationstatus';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.verificationstatus,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.verificationstatus || '';
       		}
    	};
    }
 	var columnId = 'numcoviddocs';
	if (!includeOnlyFieldList || includeOnlyFieldList.indexOf(columnId) >= 0)
	{
    	defaultColumnDefinitions[columnId] =
    	{
    	  	"aTargets": [ -1 ],
       		"sClass": "text-left",
       		"sTitle": org.r3.db.sailevent.appcrewmemberview.message.numcoviddocs,
       		"render": function ( data, type, row, meta )
       		{
          		var item = data;
          		return item.dataBean.numcoviddocs || '';
       		}
    	};
    }
 
    return defaultColumnDefinitions;
};

